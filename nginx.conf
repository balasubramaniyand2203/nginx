worker_processes 1;

events {
    worker_connections 1024;
}

http {
    proxy_headers_hash_max_size 1024;
    proxy_headers_hash_bucket_size 128;

    client_max_body_size 500M;
    proxy_connect_timeout 600s;
    proxy_send_timeout 600s;
    proxy_read_timeout 900s;
    send_timeout 600s;

    proxy_buffering off;
    ssl_prefer_server_ciphers on;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers "ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384";

    sendfile on;

    upstream docker-opexwiseui {
        server opexwiseui:80;
    }
    upstream docker-keycloak {
        server keycloak:8080;
    }

    upstream docker-jobs_api-jobapi-1 {
        server jobs_api-jobapi-1:80;
    }
    
    upstream docker-api {
        server api-opexwise-1:80;
    }

    upstream docker-grafana {
        server grafana:3000;
    }

    upstream docker-node{
        server node-exporter:9100;
    }
    
    upstream docker-cadvi{
        server 172.16.10.56:8081;
    } 

    upstream docker-prome{
        server prometheus:9090;
    }
    
    server {
        listen 443 ssl;
        server_name admin.opexwise.ai;

        ssl_certificate /etc/nginx/certs/opexwise.crt;
        ssl_certificate_key /etc/nginx/certs/opexwise.key;
      
        add_header Access-Control-Allow-Origin *;

        client_max_body_size 500M;
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
        send_timeout 600s;

        location / {
            proxy_pass http://docker-opexwiseui/;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Host $server_name;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_read_timeout 86400;
	    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
            proxy_ssl_certificate /etc/nginx/certs/opexwise.crt;
            proxy_ssl_certificate_key /etc/nginx/certs/opexwise.key;
            proxy_ssl_protocols TLSv1.2 TLSv1.3;
            proxy_set_header Access-Control-Allow-Origin *;
            proxy_ssl_ciphers "ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384";
            proxy_ssl_session_reuse on;
        }
    }

    server {
        listen 443 ssl;
        server_name keycloak.opexwise.ai;

        ssl_certificate /etc/nginx/certs/opexwise.crt;
        ssl_certificate_key /etc/nginx/certs/opexwise.key;

        client_max_body_size 500M;
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
        send_timeout 600s;

        location / {
            proxy_pass http://docker-keycloak/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Host $server_name;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_read_timeout 86400;
            proxy_set_header Access-Control-Allow-Origin *;
	    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

            proxy_ssl_certificate /etc/nginx/certs/opexwise.crt;
            proxy_ssl_certificate_key /etc/nginx/certs/opexwise.key;
            proxy_ssl_protocols TLSv1.2 TLSv1.3;
            proxy_ssl_ciphers "ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384";
            proxy_ssl_session_reuse on;
        }
    }

    server {
        listen 443 ssl;
        server_name jobapi.opexwise.ai;

        ssl_certificate /etc/nginx/certs/opexwise.crt;
        ssl_certificate_key /etc/nginx/certs/opexwise.key;

        client_max_body_size 500M;
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
        send_timeout 600s;
        
        location / {
            proxy_pass http://docker-jobs_api-jobapi-1/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Host $server_name;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_read_timeout 86400;
            proxy_set_header Access-Control-Allow-Origin *;
	    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

            proxy_ssl_certificate /etc/nginx/certs/opexwise.crt;
            proxy_ssl_certificate_key /etc/nginx/certs/opexwise.key;
            proxy_ssl_protocols TLSv1.2 TLSv1.3;
            proxy_ssl_ciphers "ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384";
            proxy_ssl_session_reuse on;
        }
    }

    server {
        listen 443 ssl;
        server_name opexapi.opexwise.ai;

        ssl_certificate /etc/nginx/certs/opexwise.crt;
        ssl_certificate_key /etc/nginx/certs/opexwise.key;

        client_max_body_size 500M;
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
        send_timeout 600s;
        
        location / {
            proxy_pass http://docker-api/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Host $server_name;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_read_timeout 86400;
            proxy_set_header Access-Control-Allow-Origin *;
	    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

            proxy_ssl_certificate /etc/nginx/certs/opexwise.crt;
            proxy_ssl_certificate_key /etc/nginx/certs/opexwise.key;
            proxy_ssl_protocols TLSv1.2 TLSv1.3;
            proxy_ssl_ciphers "ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384";
            proxy_ssl_session_reuse on;
        }
    }  

    server {
        listen 443 ssl;
        server_name grafana.opexwise.ai;

        ssl_certificate /etc/nginx/certs/opexwise.crt;
        ssl_certificate_key /etc/nginx/certs/opexwise.key;

        client_max_body_size 500M;
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
        send_timeout 600s;
        
        location / {
            proxy_pass http://docker-grafana/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Host $server_name;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_read_timeout 86400;
            proxy_set_header Access-Control-Allow-Origin *;
	    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

            proxy_ssl_certificate /etc/nginx/certs/opexwise.crt;
            proxy_ssl_certificate_key /etc/nginx/certs/opexwise.key;
            proxy_ssl_protocols TLSv1.2 TLSv1.3;
            proxy_ssl_ciphers "ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384";
            proxy_ssl_session_reuse on;
        }
    } 

    server {
        listen 443 ssl;
        server_name node-app.opexwise.ai;

        ssl_certificate /etc/nginx/certs/opexwise.crt;
        ssl_certificate_key /etc/nginx/certs/opexwise.key;

        client_max_body_size 500M;
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
        send_timeout 600s;
        
        location / {
            proxy_pass http://docker-node/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Host $server_name;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_read_timeout 86400;
            proxy_set_header Access-Control-Allow-Origin *;
            proxy_set_header X-Frame-Options "ALLOW-FROM https://cadvisor-apps.opexwise.ai/metrics";
            proxy_set_header X-Frame-Options "ALLOW-FROM https://grafana.opexwise.ai/";
            proxy_set_header X-Frame-Options "ALLOW-FROM https://prome-apps.opexwise.ai/metrics";
            add_header Content-Security-Policy "frame-ancestors 'self' https://prome-apps.opexwise.ai https://cadvisor.opexwise.ai https://node-app.opexwise.ai  ";
            add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;


            proxy_ssl_certificate /etc/nginx/certs/opexwise.crt;
            proxy_ssl_certificate_key /etc/nginx/certs/opexwise.key;
            proxy_ssl_protocols TLSv1.2 TLSv1.3;
            proxy_ssl_ciphers "ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384";
            proxy_ssl_session_reuse on;
        }
    } 

    server {
        listen 443 ssl;
        server_name cadvisor-apps.opexwise.ai;

        ssl_certificate /etc/nginx/certs/opexwise.crt;
        ssl_certificate_key /etc/nginx/certs/opexwise.key;

        client_max_body_size 500M;
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
        send_timeout 600s;
        
        location / {
            proxy_pass http://docker-cadvi/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Host $server_name;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_read_timeout 86400;
            proxy_set_header Access-Control-Allow-Origin *;
	    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

            proxy_ssl_certificate /etc/nginx/certs/opexwise.crt;
            proxy_ssl_certificate_key /etc/nginx/certs/opexwise.key;
            proxy_ssl_protocols TLSv1.2 TLSv1.3;
            proxy_ssl_ciphers "ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384";
            proxy_ssl_session_reuse on;
        }
    } 

    server {
        listen 443 ssl;
        server_name prome-apps.opexwise.ai;

        ssl_certificate /etc/nginx/certs/opexwise.crt;
        ssl_certificate_key /etc/nginx/certs/opexwise.key;

        client_max_body_size 500M;
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
        send_timeout 600s;
        
        location / {
            proxy_pass http://docker-prome/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Host $server_name;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_read_timeout 86400;
            proxy_set_header Access-Control-Allow-Origin *;
            proxy_set_header X-Frame-Options "ALLOW-FROM https://cadvisor-apps.opexwise.ai/metrics";
            proxy_set_header X-Frame-Options "ALLOW-FROM https://prome-apps.opexwise.ai/metrics";
            proxy_set_header X-Frame-Options "ALLOW-FROM https://cadvisor-airflow.opexwise.ai/metrics";
            proxy_set_header X-Frame-Options "ALLOW-FROM https://prome-airflow.opexwise.ai/metrics";
            proxy_set_header X-Frame-Options "ALLOW-FROM https://node-app.opexwise.ai/metrics";
            add_header Content-Security-Policy "frame-ancestors 'self' https://prome-apps.opexwise.ai https://cadvisor.opexwise.ai https://node-app.opexwise.ai https://cadvisor-airflow.opexwise.ai https://prome-airflow.opexwise.ai ";
            add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
            proxy_ssl_certificate /etc/nginx/certs/opexwise.crt;
            proxy_ssl_certificate_key /etc/nginx/certs/opexwise.key;
            proxy_ssl_protocols TLSv1.2 TLSv1.3;
            proxy_ssl_ciphers "ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384";
            proxy_ssl_session_reuse on;
        }
    } 
}

